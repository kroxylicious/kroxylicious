:_mod-docs-content-type: SNIPPET

== Replacement matchers

Replacement matchers control how field values are matched and transformed.
A replacement matcher is defined using this format:

[source]
----
/<matching regular expression>/<replacement>/<flags>
----
where:

* `<matching regular expression>` is the regular expression used to test the field value.
* `<replacement>` defines the replacement value to use when the regular expression is matched.
The replacement can refer to groups defined in the regular expression using `$1` for the first matching group and so on.
* `flags` (optional) can be either `L` or `U` for lower or upper-casing respectively.

