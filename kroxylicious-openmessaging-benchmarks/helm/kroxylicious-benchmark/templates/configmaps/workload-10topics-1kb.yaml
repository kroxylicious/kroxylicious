#
# Copyright Kroxylicious Authors.
#
# Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0
#

apiVersion: v1
kind: ConfigMap
metadata:
  name: omb-workload-10topics-1kb
  labels:
    app: omb-benchmark
data:
  workload.yaml: |
    # OpenMessaging Benchmark Workload Definition
    # 10 topics with 1KB messages

    name: 10topics-1partition-1kb

    # Topic configuration
    topics: 10
    partitionsPerTopic: 1

    # Message configuration
    messageSize: 1024
    payloadFile: "payload/payload-1Kb.data"

    # Subscription configuration
    subscriptionsPerTopic: 1
    consumerPerSubscription: 1

    # Producer configuration
    producersPerTopic: 1
    producerRate: {{ div .Values.benchmark.producerRate 10 }}

    # Test duration
    testDurationMinutes: {{ .Values.benchmark.testDurationMinutes }}
    warmupDurationMinutes: {{ .Values.benchmark.warmupDurationMinutes }}
