#
# Copyright Kroxylicious Authors.
#
# Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0
#

---
metadata:
  apiKeys: "TXN_OFFSET_COMMIT"
  apiVersion: 0
  scenario: "user is not allowed to READ group, filter responds with group authorization failed response"
given:
  mockedUpstreamResponses: []
  authorizerRules:
    allowed:
      - subject: "alice"
        resourceName: "my-topic"
        resourceClass: "TOPIC"
        resourceType: "READ"
      - subject: "alice"
        resourceName: "my-transactional-id"
        resourceClass: "TRANSACTIONAL_ID"
        resourceType: "WRITE"
      - subject: "alice"
        resourceName: "my-group"
        resourceClass: "GROUP"
        resourceType: "DESCRIBE" # requires READ
  topicNames: null
when:
  subject: "alice"
  requestHeader:
    requestApiKey: 28
    requestApiVersion: 0
    correlationId: 1
    clientId: "clientId"
  request:
    transactionalId: "my-transactional-id"
    groupId: "my-group"
    producerId: 525
    producerEpoch: 828
    topics:
      - name: "my-topic"
        partitions:
          - partitionIndex: 815
            committedOffset: 978
            committedMetadata: "random-string-946"
then:
  expectedResponseHeader:
    correlationId: 1
  expectedErrorResponse: "GROUP_AUTHORIZATION_FAILED"