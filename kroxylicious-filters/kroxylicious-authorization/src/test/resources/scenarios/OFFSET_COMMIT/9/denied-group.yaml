#
# Copyright Kroxylicious Authors.
#
# Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0
#

---
metadata:
  apiKeys: "OFFSET_COMMIT"
  apiVersion: 9
  scenario: "if all topics are allowed, forward to upstream"
given:
  mockedUpstreamResponses: []
  authorizerRules:
    allowed:
      - subject: "alice"
        resourceName: "my-topic"
        resourceClass: "TOPIC"
        resourceType: "READ"
  topicNames: null
when:
  subject: "alice"
  requestHeader:
    requestApiKey: 8
    requestApiVersion: 9
    correlationId: 1
    clientId: "clientId"
  request:
    groupId: "my-group"
    generationIdOrMemberEpoch: 957
    memberId: "random-string-816"
    groupInstanceId: "random-string-577"
    topics:
      - name: "my-topic"
        partitions:
          - partitionIndex: 61
            committedOffset: 825
            committedLeaderEpoch: 251
            committedMetadata: "random-string-0"
then:
  expectedResponseHeader:
    correlationId: 1
  expectedErrorResponse: "GROUP_AUTHORIZATION_FAILED"
